class ContriStatisticsEntity {
  final int total;
  final int pending;
  final int refused;
  final int approved;
  const ContriStatisticsEntity(
      {required this.total,
      required this.pending,
      required this.refused,
      required this.approved});
}
